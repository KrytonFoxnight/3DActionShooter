# 포폴 3D 마무리 TODO

> 기준일: 2026-08-25 (화) / **08-26 갱신: 적 추적을 NavMesh 기반으로 전환**
> **코드 마감: 2026-08-27(목) 오후. 이후 코드 수정 없음.**
> 서류 접수: 08-25 ~ **09-07(월) 16:00**

---

## 원칙

- **위에서부터 순서대로.** 총량 협상 안 함. 사다리로 감.
- 안 되면 아래를 버림. 순서는 안 바꿈.
- 미완성은 감추지 않고 README에 "의도적 제외"로 씀.
- 코어 코드는 AI에게 안 받음. (개념 질문·비교는 OK)

---

## 사다리

### 0. 확인 + 사망 처리 — 15~40분

- [ ] **4.5 사망 처리** — `Health` hp≤0 분기 있나
- [ ] 4.4 무적시간 — 있나 (없으면 **안 함**)
- [ ] 3.2-d 부채꼴 — 있나 (없으면 **안 함**, 원형으로 성립)

없으면 바로 구현:
- [ ] hp≤0 → 사망 진입
- [ ] 적: 비활성화 (`Destroy` 아님 — 풀링 확장 여지)
- [ ] 플레이어: 입력 차단 + 사망 모션
- [ ] `OnDeath` 이벤트 발행 → 구독자가 처리

### 1. 5.1 적 추적 이동 (NavMesh) — ??? ← **유일한 진짜 코어**

- [ ] 5.1-a `NavMeshSurface` 베이크 (빈 GO → Add Component → Bake, 5분)
- [ ] 5.1-b 적에 `NavMeshAgent` + `SetDestination(player.position)`
- [ ] 5.1-c 회전 — `agent.updateRotation` 기본값으로 됨. 안 되면 그때 손댐
- [ ] 5.1-d `stoppingDistance` / `autoBraking` ← **진동 발생 지점**
- [ ] 5.1-e 걷기 애니 — `agent.velocity.magnitude` → Animator 파라미터

**방침 (08-26 변경):** NavMesh **씀.** 직전 "벡터 직접 구현" 방침 폐기.
- 직접 구현에 포폴 이점 없음. 표준 도구 있고 실무에서 그걸 씀
- 직전 근거였던 "2.1 Cinemachine과 같은 판단"은 **유비가 부정확했음.** 카메라 커스텀은 흔하고 경로탐색 재구현은 안 흔함
- 씬이 Plane 하나 + 장애물 0개라 지금은 결과 동일. 장애물 놓는 순간 NavMesh만 회피를 보여줌

⚠️ **`CharacterController` 병용 금지.** `NavMeshAgent` 전담. (둘 다 transform 밈 → 수동 동기화 배선 생김)
**진동 나오면 원인·해결·검증 메모 남김** → 트러블슈팅 문서 재료
→ 면접 포인트가 "직접 짰다" → **"파라미터 검증하고 조정했다"**로 옮겨감

### 2. 상태 전환 — 30분

- [ ] `enum EnemyStateType { Idle, Chase, Attack, Dead }` (컴포넌트 `EnemyState`와 구분 — Boss Room `AIBrain.cs:15` 방식)
- [ ] `switch` 하나

⚠️ FSM 클래스 계층 **미리** 안 만듦. 근데 나중에 교체 가능하게 **경계 넷은 지킴:**

1. 전이는 `ChangeState(next)` 한 지점만 통과. 필드 직접 대입 금지 ← 나중에 `OnEnter`/`OnExit` 자리
2. 판단/행동 분리 — `DecideNext()`(다음 상태 반환, 부작용 X) / `Tick()`(행동). `switch` 두 개가 상한
3. 상태별 타이머·카운터는 `ChangeState`에서 초기화. 밖으로 안 흩뿌림
4. 외부엔 `CurrentState` 읽기전용 + `OnStateChanged` 이벤트만 노출

근거: Boss Room `AIBrain.cs:43-45`도 enum 안 버림. `Dictionary<AIStateType, AIState>` **키로 남김.**
인터페이스는 지금 안 만듦. 상태 4개가 뭘 필요로 하는지 아직 모름. 안 맞으면 두 번 고침.

---

**━━━ 여기까지 = 최소 합격선 ━━━**

---

### 3. 스포너 (B) — 45분

- [ ] `EnemySpawner` — 시작 시 스폰포인트에서 N마리 생성
- [ ] 스폰포인트 4~5개, **넓게 벌려 배치** (겹침 완화)

설계 훅 3번(단일 스포너 창구) 그대로. 새 개념 없음.
안 되면 → **씬에 5마리 수동 배치(A)로 대체. 0분. GIF는 똑같이 나옴.**

### 4. 체력바 — 40분 ← **판정 가시화**

- [ ] 월드스페이스 캔버스 + `LookAt(camera)` 빌보드
- [ ] `Health` 이벤트 구독 (이미 순수 C#임)

⚠️ **Screen Space 캔버스로 하지 말 것.** 좌표 변환 코드 붙음.
⚠️ 40분 넘으면 방식이 틀린 것. 멈추고 재검토.

**이유:** 이거 없으면 GIF에서 hitDelay·데미지가 안 보임. 공들인 구조가 영상에서 증발함.

---

**━━━ 여기까지 = 목표선 ━━━**

---

### 5. 전멸 감지 → 재스폰 (C) — 40분

- [ ] 적 사망 이벤트 구독 + 카운터
- [ ] 0 되면 다음 그룹 스폰

⚠️ **GIF에 안 보임** (10초짜리라 2그룹 안 들어감). 코드 샘플용.
⚠️ 겹침 심해짐. **`separation` 계산 붙이지 말 것. 반나절임.**

### 6. 킬카운트 Text — 15분

`int` + Text. 새 증거 0. 시간 진짜 남으면.

---

## 게이트

| 시점 | 조건 | 처리 |
|---|---|---|
| 8/25 밤 | 5.1-a/b/c 안 됨 | **3~6 전부 버림.** 8/26 통째로 5.1 |
| 8/26 낮 | 5.1 완료 | 3 → 4 순서로 |
| 8/26 밤 | 4까지 됨 | 5는 **8/27 오전 1시간만.** 안 되면 버림 |
| 8/26 밤 | 5.1 안 됨 | **적 추적 포기.** 현재 상태로 마감 |
| 8/27 오후 | 무조건 | **코드 중단.** 산출물로 전환 |

**8/27 오후는 못 뺌.** 산출물 없으면 3일 전체가 0임.

---

## 8/27 산출물 (코드 안 씀)

- [ ] **GIF ①** 이동 + 카메라 추적
- [ ] **GIF ②** 적 추적 → 접근 → 공격
- [ ] **GIF ③** 공격 → 피격 → 사망
- [ ] 스크린샷 1장
- [ ] **핵심 코드 위치 목록**
  `PlayerMeleeAttack` / `AttackConfig` / `Health` / `PlayerHealth` / 적 AI / `EnemySpawner`
- [ ] **README** — 범위 + 의도적 제외 + 실행 방법 + 조작법
- [ ] 설계 문서 2건 공개 (플레이어 구조 / hitDelay)
- [ ] dev → main PR 병합
- [ ] `planning-guideline.md` 맨 위에 동결 한 줄 추가

### README 문구 (그대로 씀)

```md
범위는 전투 판정 사이클까지입니다.

구현: 3D 이동 / 카메라 컨트롤러(직접 구현) / 근접 공격 판정 /
      hitDelay 기반 판정-표현 분리 / 피격·사망 / 근접 적 추적 AI /
      스폰포인트 기반 적 생성 / 적 체력 표시 / 대시

미구현(의도적 제외): 웨이브 데이터 구성, 보상 선택, 결과 화면
→ UI·데이터 반영 영역은 이 프로토타입의 관심사(엔진 영역)를 벗어나므로
  제한된 기간 내 집중을 위해 범위에서 제외했습니다.
```

---

## 그 다음

```
08-28        레포 정리 (CLAUDE.md 쪼개기 / .private / 히스토리)
08-28~09-05  자소서 + 포트폴리오. 대표는 실무 라이브 게임
09-06        점검
09-07 오전   제출  ← 16시 마감. 오후 아님
```

---

> 우리 노력함. 우리 안 쫓김. 우리 안 포기함.
