# CLAUDE.md

이 문서는 Claude Code가 이 저장소에서 작업할 때 따라야 할 프로젝트 지침이다.

> **최종 갱신: 2026-09-06** — 촬영용 연출 작업으로 코드를 재개했고, README·GIF를 완료했다.
> 직전 버전(코드 작업 종료 08-29 / UI 제외)은 **더 이상 유효하지 않다.** 09-06에 HUD와 카메라 흔들림을 구현했다.

---

## ⚠️ 0. 현재 상태 (가장 먼저 읽을 것)

### 이 프로젝트의 위치

**공개 가능한 보조 프로젝트다. 대표 프로젝트가 아니다.**
따라서 이 프로젝트가 증명해야 하는 것은 "완성된 게임"이 아니라 다음 세 가지다.

1. Unity 엔진 영역(이동·카메라·물리 판정·적 AI)을 직접 다뤘다는 **공개 코드**
2. 설계 선택의 근거를 남겼다는 **설계 결정 문서**
3. AI를 쓰되 검증했다는 **AI 사용 원칙과 그 증거**

### 현재 단계

**저장소 작업 마무리 단계다. 남은 것은 `dev` → `main` 병합 하나다.**

코드 작업은 2026-08-29에 한 번 종료했으나 **2026-09-06에 재개했다.**
GIF를 찍기 직전에 화면만으로는 시스템 동작을 읽을 수 없다는 것이 드러났기 때문이다.
`Cleared` / `Failed`는 상태만 있고 화면에 나오지 않았고, 플레이어 자신의 체력도 표시 수단이 없었다.
**8절 기준 3번(GIF에서 보이는가)을 통과하지 못하는 상태였다.** 근거와 범위는 2절에 있다.
README와 GIF 3장은 완료했다. 상세는 3절에 있다.

---

## 1. 프로젝트 개요

**Unity 3D ARPG 전투 판정 프로토타입**

- 3인칭(마우스 시점) 근접 무기 ARPG의 전투 판정 사이클을 다루는 프로토타입.
    - (2026-07-18) 원래 탑다운으로 고정했으나 1주차에 3인칭으로 전환. 이후 이동·조준·애니메이션 설계는 그 전제를 따른다.
    - (2026-08-25) 범위를 **전투 판정 사이클까지**로 축소 확정.
    - (2026-08-29) 웨이브 진행까지 구현하고 코드 작업을 종료. **원거리 무기는 구현하지 않았다.** 근접만 있다.
- 싱글 플레이. 플레이어블 데모가 아니라 **시스템 프로토타입**이다.
- 특정 상용 게임의 IP·캐릭터·아트·UI·명칭·세계관을 모방하지 않는다.

### 프로젝트의 존재 이유

사용자의 기존 실무 강점은 UGUI, MVVM/ViewController, VContainer, 상점/IAP, 효과 시스템, 테스트 코드, CI/CD 등 라이브 서비스형 클라이언트 시스템이다. 이 프로젝트는 그 강점을 반복하지 않고, 공개 증거가 부족한 영역만 보완한다:

- 3D 공간 활용, 캐릭터 이동, 물리/충돌/피격 판정
- 카메라 제어, 애니메이션 상태 전환
- 적 AI (추적 / 상태 전환)

**UI·데이터 반영·라이브 서비스 기능은 실무에서 이미 증명된 영역이므로 이 저장소에서 다루지 않는다.**

> **09-06 예외 하나.** GIF에서 시스템 동작을 읽게 하는 HUD(플레이어 체력 · 웨이브 정보 · 결과 배너)는 구현했다.
> 게임 UI를 만든 것이 아니라 **동작 증거를 만든 것**이다. 판정·웨이브 진행은 이미 구현돼 있었고 화면에 드러나지 않았을 뿐이다.
> 이 예외는 여기서 끝난다. 보상 UI·상점·데이터 반영으로 넓히지 않는다.

---

## 2. 현재 범위 (확정)

### 구현 대상 — 코드 작업 종료 (2026-08-29)

| 항목 | 상태 |
|---|---|
| 플레이어 이동 (camera-relative), 입력 처리 | 완료 |
| 3인칭 카메라 컨트롤러 (직접 구현, Cinemachine 미사용) | 완료 |
| 대시 / 점프 | 완료 |
| 근접 기본 공격 (`PlayerMeleeAttack`) | 완료 |
| `AttackConfig` SO + `hitDelay` 기반 판정 시점 분리 | 완료 |
| HP (`Health` 순수 C#) / 피격 (`IDamageable`) | 완료 |
| 사망 처리 — 플레이어·적 양쪽 | 완료 |
| 유닛 상태 권한자 (`PlayerCharacter` / `EnemyCharacter`) | 완료 |
| 적 추적 이동 — NavMesh (`EnemyMovement`) | 완료 |
| 적 상태 전환 — `enum` + `switch` 3상태 (`EnemyAI`) | 완료 |
| 적 근접 공격 / 피격 경직 | 완료 |
| 적 스폰 (`EnemySpawner` / `EnemySpawnPoint` / `EnemySpawnRequest`) | 완료 |
| 체력바 UI — 스크린스페이스 투영 | 완료 |
| 웨이브 진행 (`GameDirector` 상태 머신) | 완료 |
| `LogManager` 로그 중앙화 + asmdef 분리 | 완료 |
| HUD — 플레이어 체력 · 웨이브 정보 · 결과 배너 (09-06) | 완료 |
| 피격 시 카메라 흔들림 (09-06) | 완료 |
| 무적 토글 개발 기능 (09-06) | 완료 |
| 웨이브 데이터의 적 표시 이름 (09-06) | 완료 |

> **범위가 계획을 넘었다.** 직전 버전의 이 문서는 웨이브를 동결 항목으로 두었으나 08-28~29에 구현했다.
> 스포너와 전멸 감지가 끝난 뒤 남은 것이 상태 머신 하나뿐이었기 때문이다. 근거는 `docs/decisions/wave-and-enemy-cleanup.md`.
> 이 때문에 코드 마감이 08-27에서 08-29로 밀렸다.

> **제거한 것:** 학습 단계에서 쓰던 `TrainingDummy` 계층(스크립트 7종·프리팹·전용 에셋)을 08-30에 삭제했다.
> `Enemy` 계층 구현 완료로 역할이 끝났고, 타입명만 다른 중복 코드로 남아 있었다.

### 동결 (구현하지 않음)

아래는 "못 한 것"이 아니라 **판단에 따라 범위에서 제외한 것**이다. README에도 그렇게 기술한다.

- **보상 선택 / 스탯 강화** — UI + 데이터 반영. 실무 강점 영역과 중복이므로 제외
- **결과 화면** — 09-06에 HUD 배너 한 줄(`ALL WAVES CLEARED` / `DEFEATED`)만 추가했다. 전용 결과 화면은 만들지 않는다
- 원거리 적, 미니 보스, 스킬 2종, 락온
- Object Pooling, VFX, 전투 로그
- 부채꼴 각도 제한 (3.2-d) — 원형 판정으로도 프로토타입 성립
- 카메라 오클루전 (2.4)
- **피격 무적 시간 (4.4)** — i-frame 규칙은 구현하지 않았다.
  `PlayerCharacter.cs:24`의 `invincible`은 **디버그용 플래그**이며 무적 시간 시스템이 아니다.
  09-06에 화면 버튼으로 토글할 수 있게 했으나 성격은 그대로다. 이 둘을 README에서 혼동해 적지 않는다

### 동결 — 네트워크

- **1단계(싱글) 유지. 네트워크 코드는 넣지 않는다.**
- **2단계(NGO 2인 코옵)는 무기한 동결.** 남은 일정에 착수 여지가 없고, 이 저장소가 증명하려는 것(0절)에서도 벗어난다.
- 단, 이미 반영된 **설계 훅 3개는 그대로 유지한다.** 네트워크 대비가 아니더라도 좋은 구조이며, 설명 가능한 자산이다.
    1. 입력 처리와 행동 로직을 분리한다 (입력 → 의도/커맨드 → 실행).
    2. 데미지/스탯 계산은 MonoBehaviour에 얽매이지 않는 순수 C# 레이어로 둔다.
    3. 적/투사체 스폰은 팩토리(또는 단일 스포너 창구)를 거친다.
- 이 훅을 넘어서는 선제적 네트워크 추상화는 금지.

### 동결 — 멀티스레딩 (Job System / Burst)

- **적용하지 않는다.** 유력 후보였던 "Wave 3 다수 적의 조향 계산"과 "투사체 일괄 처리"가 모두 동결 범위에 들어가면서 **적용 근거가 사라졌다.**
- 소수 오브젝트에 Jobs를 넣는 것은 원래 판단 기준("메인 스레드에서 충분히 싼 계산에 억지로 넣지 않는다")에 위배된다.
- 물으면: 적용 후보와 함께 **왜 적용하지 않았는지**를 답한다. 이것도 판단의 증거다.

---

## 3. 잔여 작업 (2026-08-30 ~)

**코드 작업은 끝났다. 남은 것은 전부 포장이다.**

```
① dev → main PR 병합        ← 유일하게 남은 작업
② README 작성               ← 완료 (09-06)
③ GIF 3장 (docs/media/)     ← 완료 (09-06)
④ (선택) Health 유닛 테스트  ← 하지 않는다
```

### 왜 이 순서인가

- `main`이 공개 저장소의 기본 화면이다. 여기가 낡아 있으면 NavMesh AI·웨이브·체력바가 **공개돼 있지 않은 것과 같다.**
- README가 없으면 설계 문서 6건을 아무도 찾지 못한다. 30초 안에 보이는 것이 파일 목록뿐이다.
- 아트가 private 서브모듈이라 클론해도 화면이 비어 있다. **GIF가 유일한 동작 증거다.**

### WebGL 빌드 배포를 하지 않는 이유 (09-06 판단)

검토했고 **범위에서 제외했다.** 금전 비용은 0원이지만 시간이 반나절~하루이고, 그 견적에 구멍이 있다.

- WebGL 모듈 미설치, 커서 락(`Cursor.lockState`) 미구현 — 마우스 시점이라 이게 없으면 브라우저에서 플레이가 성립하지 않는다
- URP 첫 빌드의 셰이더 컴파일과 오류 대응은 얼마가 될지 읽히지 않는다
- **1절이 이미 "플레이어블 데모가 아니라 시스템 프로토타입"이라고 못박았다.** 실행 가능한 빌드는 0절의 증명 대상이 아니다
- 9절이 동작 증거를 GIF로 정한 결정도 그대로 유효하다

README에는 "준비 중"이 아니라 **제외 사유**로 적는다. 판단을 누락처럼 보이게 두지 않는다.

### (선택) Health 유닛 테스트

`Health`를 순수 C#으로 분리해 놓고 테스트가 0개다. "테스트 가능하게 설계했다"는 논리의 결론이 비어 있다.
EditMode asmdef 하나 + 케이스 6개면 닫힌다: 음수 / 0 / 현재 체력 초과 / 고갈 후 추가 데미지 / `ResetToFull` / `Changed` 발행 횟수.

### 컷 조건

- **①②③이 끝나면 저장소 작업을 종료한다.** ④는 시간이 남을 때만 한다.
- 저장소 작업이 끝나면 다른 작업으로 넘어간다. 이 저장소에 일정을 더 쓰지 않는다.
- **새 기능은 추가하지 않는다.** 8절 기준을 그대로 적용한다.

---

## 4. AI 사용 원칙 (2026-07-31 확정 / **2026-08-26 개정 — 학습 모드 종료**)

> AI로 코드를 쓰는 것 자체는 문제가 아니다. 문제는 AI가 만든 코드를 자기 실력으로 착각하는 것이다.
> 따라서 중요한 것은 "썼는가"가 아니라 **"검증하고 설명할 수 있는가"** 다.

### Claude Code에게 적용되는 규칙

> **2026-08-26 개정.** 직전 규칙("게임플레이 코어 구현 코드를 대신 작성하지 않는다")은 **폐기한다.**
> 그 규칙은 사용자가 3D/엔진 영역을 **학습하던 단계**의 장치였고, `guide` 스킬로 라인 단위 지시를 받아 손으로 옮겨 적는 방식이었다.
> 학습 단계는 끝났다. 지금은 작업 단계이며, `guide` 스킬은 이 프로젝트에서 제거했다.

- ✅ **구현 코드를 직접 작성한다.** 코어 영역(플레이어 이동·공격 판정·피격·사망·적 AI·상태 전환)도 포함한다.
  - 먼저 만든다. 라인 단위 지시서를 만들어 사용자가 옮겨 적게 하지 않는다.
  - 사용자가 **별도로 물어본 부분만** 설명한다. 묻지 않은 것까지 선제적으로 해설하지 않는다.
- ✅ 제한 없음: README, 회고 글, 설계 문서, 커밋 메시지, 문서 정리
- ✅ 제한 없음: 에셋 임포트, 설정, 빌드 등 `chore` 성격 작업

**판단 기준은 그대로다.** *"이거 왜 이렇게 구현했냐"* 를 물었을 때 사용자가 설명할 수 있어야 한다.
바뀐 것은 **기준이 아니라 그 기준을 채우는 순서**다. 사전 가이드 → 손코딩이 아니라, **구현 → 사용자 질의 → 설명**으로 확보한다.

따라서 다음은 유지된다.
- 설계 선택지가 갈리는 지점(예: `enum` + `switch` vs FSM 계층, NavMesh vs 직접 구현)은 **구현 전에 근거와 대안을 밝힌다.** 사용자가 그 판단을 설명할 수 있어야 하기 때문이다.
- 사용자가 "왜 이렇게 짰냐"고 물으면 코드 위치(`file:line`)와 함께 답한다.
- 설명할 수 없는 코드가 저장소에 남으면 이 프로젝트는 여전히 감점 요인이 된다. 그 책임은 사라지지 않았고, 확인 시점만 뒤로 옮겼다.

---

## 5. 설계 결정 문서 (핵심 산출물)

이 저장소의 **코드보다 중요한 산출물**이다. 채택/기각/근거/레퍼런스 구조를 유지한다.

1. **플레이어 구조 결정: 순수 C# 중앙화 vs 기능 단위 컴포넌트 분리** (08-01) — `docs/decisions/component-architecture.md`
    - 채택: 방식 B(기능 단위 MonoBehaviour 분리 + 계산·규칙만 순수 C#)
    - 근거: 이 프로젝트는 엔진 기능이 코어, 실무 프로젝트는 시뮬레이션이 코어. 조건이 반대
    - 레퍼런스: Chop Chop, Boss Room 모두 전면 순수 C# 아님

2. **공격 판정-애니메이션 타이밍 관리: 데이터 주도(hitDelay)** (08-02) — `docs/decisions/attack-timing.md`
    - 채택: `AttackConfig` SO의 `hitDelay` + 코루틴 타이머 / 기각: Animation Event 주도
    - 근거: 서버 권위 구조 이식성, 1인 개발에서 Animation Event의 장점 무의미, 판정/표현 분리
    - 레퍼런스: Boss Room `ActionConfig.cs` / `MeleeAction.cs`

3. **유닛 상태 관리 권한: 분산 소유 vs 권한자 컴포넌트** (08-26) — `docs/decisions/unit-state-authority.md`
    - 채택: 유닛마다 상태 소유·변경 권한을 독점하는 컴포넌트 (`PlayerCharacter` / `EnemyCharacter`)
    - 근거: 상태 필드 직접 대입 지점이 흩어져 있어 상태 하나 추가에 소비처 N곳을 고쳐야 함
    - 레퍼런스: Boss Room `ServerCharacter.cs` / Chop Chop `Protagonist.cs`
    - 방식 A 회귀가 아님: 중앙화 대상이 **로직**이 아니라 **상태 소유권**

4. **적 스폰 구조 결정** (08-27) — `docs/decisions/enemy-spawn.md`
    - 채택: 스폰 지점(`EnemySpawnPointProvider`) / 생성(`EnemySpawner`) / 요청 DTO(`EnemySpawnRequest`) / 지휘(`GameDirector`) 분리
    - 설계 훅 3번(스폰은 단일 창구를 거친다)의 실현

5. **캐릭터 종속 UI: 스크린스페이스 투영 vs 월드스페이스 Canvas** (08-27) — `docs/decisions/world-space-ui.md`
    - 채택: 씬 공용 스크린스페이스 Canvas + `WorldToScreenPoint` 투영
    - **계획을 뒤집은 결정이다.** `docs/progress-log.md`는 월드스페이스를 지시했다

6. **웨이브 진행과 사망 적 정리 구조** (08-28) — `docs/decisions/wave-and-enemy-cleanup.md`
    - 채택: 파괴는 적 자신이 지연 예약 / 전멸 판정은 `GameDirector`가 목록 재계산
    - 기각: 적의 `Died` 이벤트를 전멸 판정 근거로 사용

### 미작성

- **카메라를 Cinemachine 없이 직접 구현한 이유** — 가장 먼저 물어볼 지점이다. 결정은 있으나 결정 문서가 없다.
  `docs/notes/unity-camera-system.md`는 외부 세션 정리 노트이지 이 저장소의 결정 기록이 아니다.
- **적 추적 정지 거리 진동 문제** — 진동이 실제로 발생하지 않아 작성하지 않는다.
  대신 `EnemyAI.cs:15,107`의 히스테리시스로 상태 경계 떨림을 막았다는 사실이 README의 설명 대상이다.

---

## 6. 기술 환경

- **Unity 6000.3.16f1** (URP — PC/Mobile 렌더러 구성)
- 새 Input System (`InputSystem_Actions.inputactions`)
- 줄바꿈: 전체 LF 통일 (`.gitattributes`, `.editorconfig` 적용됨). Windows/Mac 크로스 작업 전제.
- 에셋: `Assets/ThirdParty/` **private 서브모듈**(`3DActionShooter-Assets`)로 분리.
  Asset Store EULA상 원본 재배포가 금지되므로 공개 저장소에 넣지 않는다. 구성은 `Assets/ThirdParty/README.md`가 정본이다.
  → 공개 저장소를 클론하면 모델·애니메이션이 비어 있다. **README에 이 사실을 반드시 명시한다.**
- Burst/Collections/Mathematics는 설치되어 있으나 **사용하지 않는다** (2절 참고).

## 7. 참고 레퍼런스 프로젝트 (로컬 클론)

| 프로젝트 | 로컬 경로 | 용도 |
|---|---|---|
| Chop Chop (Unity Open Project #1) | `../open-project-1` | 아키텍처 참고 — InputReader(SO) 입력 분리, SO 이벤트, SO 상태 머신, 풀링 |
| Boss Room | `../com.unity.multiplayer.samples.coop` | 전투·어빌리티·폴더 구조 참고. **NGO 부분은 동결 범위** |

- 두 클론은 **읽기 전용**이다. 수정·커밋하지 않고, 서브모듈로 편입하지도 않는다.
- 코드를 통째로 복사하지 않는다. 패턴만 참고해 이 프로젝트 규모에 맞게 직접 구현한다.
- Chop Chop은 2021년 12월 중단된 프로젝트다. 구조는 참고하되 API는 현재 Unity 버전 기준으로 검증한다.

---

## 8. 기능 추가 판단 기준

> **코드 작업은 2026-08-29에 종료했고, 09-06에 한 번 재개한 뒤 다시 종료했다. 기본값은 "추가하지 않는다"이다.**
> 아래 기준은 예외를 검토할 때만 쓴다.

> **09-06 재개는 이 기준을 통과했다.** HUD·결과 배너·웨이브 정보는 기준 3번(GIF에서 보이는가)을 **직접** 충족한다.
> 이미 구현된 상태(`Cleared` / `Failed`, 플레이어 체력)가 화면에 안 나오던 것을 드러낸 작업이지 새 기능이 아니었다.
> 반면 같은 자리에서 제안된 **게임 시작 전 카메라 무빙은 기각했다.** 순수 연출이라 기준 1·5를 못 채운다.
> 재개의 기준은 "있으면 좋은가"가 아니라 **"산출물 명세가 요구하는데 화면에 없는가"** 였다.

1. 이 기능이 3D 게임플레이 역량을 보여주는가?
2. **종료한 코드 작업을 다시 여는 값어치가 있는가?**
3. 스크린샷/GIF에서 보이는가?
4. 직접 설명할 수 있는가?
5. 기존 실무 경험과 중복되지 않고 약점을 보완하는가?

명확히 답하지 못하면 제외한다. **범위 확장 제안이 나오면 이 기준으로 반대 의견을 낸다.**

---

## 9. 산출물

플레이 영상 1분은 **취소한다.** 서사가 아니라 시스템 단위를 보여주는 것이 목적이므로 짧은 GIF로 대체한다.

| 산출물 | 상태 |
|---|---|
| README | 완료 (09-06) |
| GIF ① 이동 + 카메라 추적 + 점프 + 구르기 | 완료 — `docs/media/01-movement.gif` |
| GIF ② 웨이브 시작 → 적 추적 → 접근 → 공격 (체력바 감소) | 완료 — `docs/media/02-combat.gif` (README 히어로) |
| GIF ③ 피격 → 사망 → 웨이브 Failed | 완료 — `docs/media/03-death.gif` |
| 핵심 코드 위치 목록 | README에 표로 포함 |
| 설계 결정 문서 6건 공개 | 완료 (13절 표 참고) |
| `dev` → `main` PR 병합 | **미완. 유일하게 남은 작업** |
| WebGL 빌드 배포 | **하지 않는다.** 3절에 제외 사유 기록 |

### README에 반드시 들어갈 것

1. **범위와 의도적 제외**를 이유와 함께 쓴다. 미구현을 감추지 않는다.
2. **핵심 코드 위치를 `file:line`으로** 준다. 파일명 나열이 아니라 "무엇의 증거인지"를 붙인다.
3. **에셋 서브모듈이 private라 클론해도 아트가 비어 있다는 사실**을 명시한다.
   Asset Store EULA상 재배포가 금지되어 분리한 것이며(`Assets/ThirdParty/README.md`), 이는 변명이 아니라 라이선스를 알고 대응했다는 기록이다.
4. 실행 환경은 **Unity 6000.3.16f1**.
5. 조작법: `WASD` 이동(카메라 상대) / `LCtrl` 걷기·달리기 전환 / 마우스 시점 / 좌클릭 공격 / `LShift` 구르기 / `Space` 점프 / 화면 버튼 Wave Start·Reset·무적 토글(개발용).

---

## 10. 브랜치 전략

```text
main ← dev ← feature/*
```

- **main**: 항상 플레이 가능하고 보여줄 수 있는 상태만 유지한다. dev에서 **PR로만** 병합. 직접 커밋 금지.
- **dev**: 통합 브랜치. 모든 기능 개발의 출발점이자 병합 대상.
- **작업 브랜치**: dev에서 파생, 작업 하나 수행 후 dev로 병합. 짧게 유지(1~3일).
- 접두사는 5종으로 **한정**한다: `feature/` `fix/` `refactor/` `chore/` `docs/`
- **마무리 시 dev → main PR을 만들고 병합한다.** 공개 저장소의 기본 브랜치가 최신 상태여야 한다. 현재 `main`은 76커밋 뒤처져 있다.
- 머신(Windows ↔ Mac) 전환 전에는 작업 중인 브랜치를 반드시 push한다.

## 11. 워크트리 병렬 작업과 서브모듈

- 본진(`3DActionShooter`)은 사용자 전용, `-wt1`/`-wt2`는 Claude 병렬 세션용 반영구 워크트리다. 제거하지 않는다.
- `Assets/ThirdParty` 서브모듈은 워크트리마다 별도 클론이 정상이다. junction/폴더 공유 금지 — 진실은 각 브랜치의 포인터이고, 체크아웃은 파생물이다.
- 브랜치 전환/pull/push 시 서브모듈은 자동 동기화된다 (`submodule.recurse`, `push.recurseSubmodules on-demand`).
- 에셋 수정은 한 체크아웃에서만 한다. 나머지 워크트리에서 ThirdParty는 읽기 전용이며, 전파는 서브모듈 push → 부모 `chore` 커밋(포인터 갱신)으로만 한다.
- 새 워크트리 생성 시 1회 필수: `git submodule update --init --reference <본진 경로>/Assets/ThirdParty`

## 12. AgentDoc 규칙

`[AgentDoc]`은 코드의 목적·수정 시 주의사항을 사람이 남기고 AI가 읽는 어트리뷰트다 (`Assets/Scripts/Core/AgentDocModule/AgentDocAttribute.cs`). 조회는 `agent-doc` 스킬로 한다.

- **`[AgentDoc]`은 사람만 수동 작성한다. AI는 추가·수정·삭제 금지.** 유일한 예외: 사용자가 직접 `/agent-doc-bootstrap`을 호출한 경우 AI의 **신규 추가만** 허용.
- 코드 수정 전 대상 영역에 `warning`이 있으면 반드시 읽는다.
- **Scripting Define Symbols에 `AGENT_DOC`을 추가하지 않는다** — 어트리뷰트 문자열이 출시 바이너리에 유출된다. 미정의가 정상이며 스캐너는 심볼과 무관하게 소스 텍스트를 읽는다.
- `.llm-index/`는 스캔 산출물이다 (gitignore 대상). 커밋하지 않고 사용 직전 재스캔으로만 최신화한다.

---

## 13. 관련 문서의 유효성

문서는 용도로 나눈다. **새 설계 결정은 `docs/decisions/`에 짧은 이름으로 만든다** — 폴더가 이미 "결정"을 뜻하므로 `-decision` 접미사를 붙이지 않는다.

```text
docs/decisions/   설계 결정. 채택/기각/근거/레퍼런스 구조
docs/notes/       조사·운영 메모. 판단 근거로 쓰지 않는다
docs/media/       README용 GIF·스크린샷
docs/progress-log.md   진행 로그
```

| 문서 | 상태 |
|---|---|
| **CLAUDE.md (이 문서)** | **현행. 범위·일정·원칙의 유일한 기준** |
| `docs/decisions/component-architecture.md` | 현행. 설계 결정 ① |
| `docs/decisions/attack-timing.md` | 현행. 설계 결정 ② |
| `docs/decisions/unit-state-authority.md` | 현행. 설계 결정 ③ (08-30 이름 이력 주석 추가) |
| `docs/decisions/enemy-spawn.md` | 현행. 설계 결정 ④ |
| `docs/decisions/world-space-ui.md` | 현행. 설계 결정 ⑤ |
| `docs/decisions/wave-and-enemy-cleanup.md` | 현행. 설계 결정 ⑥ |
| `docs/notes/unity-camera-system.md` | 현행. 단 외부 세션(NDC) 정리 노트이며 이 저장소의 구현 결정 문서가 아니다 |
| `docs/progress-log.md` | **진행 로그로 종료.** 계획 문서로 쓰지 않는다 |
| `docs/notes/idea-backlog.md` / `docs/notes/asset-repo-management.md` | 보조 메모. 판단 근거로 쓰지 않는다 |
| 외부 개인 노트 | **저장소에 없다.** 이 저장소의 기준이 아니다 |

**충돌 시 이 문서를 따른다.**

---

## 14. 작업 원칙

- **범위를 넓히지 않는다.** 코드 작업은 끝났고, 기본값은 "추가하지 않는다"이다.
- **구현 코드를 손으로 쓰던 규칙은 2026-08-26에 해제했다 (4절).**
  그 규칙의 목적은 3D·엔진 영역을 이해하고 체화하기 위한 손코딩이었고, 지금은 그 단계를 넘겼다. 이후 작업은 직접 작성이 필요한 성격이 아니다.
  바뀐 것은 **작성 주체**이지 **기준**이 아니다.
- 설명할 수 없는 코드는 저장소에 넣지 않는다.
- 미구현은 감추지 않고 이유와 함께 명시한다.
- 과도한 추상화를 만들지 않는다. `enum` + `switch`로 충분한 곳에 계층을 만들지 않는다.
- 기획이 흔들리면 원칙으로 돌아간다: **전투 판정 사이클까지. 코드 종료. 보조 프로젝트.**
