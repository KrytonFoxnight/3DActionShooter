# CLAUDE.md

이 문서는 Claude Code가 이 저장소에서 작업할 때 따라야 할 프로젝트 지침이다.

> **최종 갱신: 2026-08-25** — 2026 넥토리얼 공식 전형 공개에 따라 범위·일정을 전면 재설정했다.
> 직전 버전(마감 08-31 / 웨이브·보상 포함)은 **더 이상 유효하지 않다.**

---

## ⚠️ 0. 현재 상태 (가장 먼저 읽을 것)

### 확정 사실

- **접수: 2026-08-25(화) ~ 09-07(월) 16:00**
- 전형: 서류 → AI면접(09-12) → AI활용 역량평가(10-03) → 직군면접(10월) → 팀면접(11월)
- **코딩테스트 없음.** 대신 AI와 1:1 대화형 면접(50분)과 AI활용 역량평가가 있다.

### 이 프로젝트의 새 위치

이 저장소는 더 이상 **대표 포트폴리오가 아니다. 보조 프로젝트다.**
대표 프로젝트는 실무 라이브 게임(출시·실매출·콘텐츠 아키텍처 0→1)이다.

따라서 이 프로젝트가 증명해야 하는 것은 "완성된 게임"이 아니라 다음 세 가지다.

1. Unity 엔진 영역(이동·카메라·물리 판정·적 AI)을 직접 다뤘다는 **공개 코드**
2. 설계 선택의 근거를 남겼다는 **설계 결정 문서**
3. AI를 쓰되 검증했다는 **AI 사용 원칙과 그 증거**

### 코드 작업 데드라인

**2026-08-27(목) 저녁. 이후 코드 수정 없음.**
08-28부터 09-07까지는 자기소개서·포트폴리오 작성 기간이며, 이 저장소는 산출물 링크로만 쓰인다.

---

## 1. 프로젝트 개요

**Unity 3D 전투 판정 시스템 프로토타입** (저장소 가칭: Descendant Arena Lite)

- 넥슨게임즈 퍼스트 디센던트를 **구조적 레퍼런스**로 삼은 3D TPS(3인칭 마우스 시점) 무기(활/검) 액션 프로토타입
    - (2026-07-18) 원래 탑다운으로 고정했으나 1주차에 TPS로 전환. 이후 이동·조준·애니메이션 설계는 TPS 전제를 따른다.
    - (2026-08-25) 범위를 **전투 판정 사이클까지**로 축소 확정. 웨이브·보상은 동결.
- 싱글 플레이. 플레이어블 데모가 아니라 **시스템 프로토타입**으로 성격을 재정의한다.
- 원작의 IP/캐릭터/아트/UI/명칭/세계관은 모방하지 않는다.

### 프로젝트의 존재 이유

사용자의 기존 실무 강점은 UGUI, MVVM/ViewController, VContainer, 상점/IAP, 효과 시스템, 테스트 코드, CI/CD 등 라이브 서비스형 클라이언트 시스템이다. 이 프로젝트는 그 강점을 반복하지 않고, 공개 증거가 부족한 영역만 보완한다:

- 3D 공간 활용, 캐릭터 이동, 물리/충돌/피격 판정
- 카메라 제어, 애니메이션 상태 전환
- 적 AI (추적 / 상태 전환)

**UI·데이터 반영·라이브 서비스 기능은 실무에서 이미 증명된 영역이므로 이 저장소에서 다루지 않는다.**

---

## 2. 현재 범위 (확정)

### 구현 대상 — 여기까지만

| 항목 | 상태 |
|---|---|
| 플레이어 이동 (camera-relative), 입력 처리 | 완료 |
| 3인칭 카메라 컨트롤러 (직접 구현, Cinemachine 미사용) | 완료 |
| 대시 | 완료 |
| 근접 기본 공격 (`PlayerMeleeAttack`) | 완료 |
| `AttackConfig` SO + `hitDelay` 기반 판정 시점 분리 | 완료 |
| HP (`Health` 순수 C#) / 피격 (`PlayerHealth : IDamageable`) | 완료 |
| 사망 처리 (hp ≤ 0) | 확인·마무리 대상 |
| **적 추적 이동 (5.1) — NavMesh 기반** | **잔여 코어 작업. 최우선** |
| 적 상태 전환 (`enum` + `switch`, 4상태) | 5.1 완료 시 착수 |

### 동결 (구현하지 않음, 삭제도 하지 않음)

아래는 "못 한 것"이 아니라 **판단에 따라 범위에서 제외한 것**이다. README에도 그렇게 기술한다.

- **웨이브 진행 시스템** — 스폰·클리어 판정·다음 웨이브
- **보상 선택 / 스탯 강화** — UI + 데이터 반영. 실무 강점 영역과 중복이므로 제외
- **결과 화면**
- 원거리 적, 미니 보스, 스킬 2종, 락온
- Object Pooling, VFX, 전투 로그
- 부채꼴 각도 제한 (3.2-d) — 원형 판정으로도 프로토타입 성립
- 피격 무적 시간 (4.4)
- 카메라 오클루전 (2.4)

### 동결 — 네트워크

- **1단계(싱글) 유지. 네트워크 코드는 넣지 않는다.**
- **2단계(NGO 2인 코옵)는 무기한 동결.** 9월은 서류 마감(09-07), AI면접(09-12), AI활용 역량평가(10-03)로 채워져 착수 여지가 없다.
- 단, 이미 반영된 **설계 훅 3개는 그대로 유지한다.** 네트워크 대비가 아니더라도 좋은 구조이며, 면접에서 설명 가능한 자산이다.
    1. 입력 처리와 행동 로직을 분리한다 (입력 → 의도/커맨드 → 실행).
    2. 데미지/스탯 계산은 MonoBehaviour에 얽매이지 않는 순수 C# 레이어로 둔다.
    3. 적/투사체 스폰은 팩토리(또는 단일 스포너 창구)를 거친다.
- 이 훅을 넘어서는 선제적 네트워크 추상화는 금지.

### 동결 — 멀티스레딩 (Job System / Burst)

- **적용하지 않는다.** 유력 후보였던 "Wave 3 다수 적의 조향 계산"과 "투사체 일괄 처리"가 모두 동결 범위에 들어가면서 **적용 근거가 사라졌다.**
- 소수 오브젝트에 Jobs를 넣는 것은 원래 판단 기준("메인 스레드에서 충분히 싼 계산에 억지로 넣지 않는다")에 위배된다.
- 면접에서 물으면: 적용 후보와 함께 **왜 적용하지 않았는지**를 답한다. 이것도 판단의 증거다.

---

## 3. 잔여 작업 (2026-08-25 ~ 08-27)

```
08-25  ❓3개 확인(15분) → 사망 처리 마무리 → 적 추적 5.1-a/b/c
08-26  5.1-d(정지 거리) / 5.1-e(걷기 애니) → 상태 전환 4상태
08-27  코드 작성 중단. 산출물만: GIF 3장 · 코드 위치 정리 · README · 설계 문서 공개
```

### 컷 조건

- 08-26 밤까지 5.1이 동작하지 않으면 **적 추적을 포기하고 현재 상태로 마감한다.**
- 08-27 저녁에는 상태와 무관하게 코드 작업을 종료한다.
- **미완성 항목은 감추지 않고 README에 "의도적 제외" 또는 "미구현"으로 명시한다.**

### 5.1 구현 방침

> **2026-08-26 변경.** 직전 방침("NavMesh를 사용하지 않는다. 벡터 연산으로 직접 구현")은 **폐기한다.**

- **NavMesh(`com.unity.ai.navigation` 2.0.12)를 사용한다.** `NavMeshSurface` 베이크 + `NavMeshAgent`.
    - 근거 ①: **직접 구현에 포트폴리오 이점이 없다.** 업계 표준 도구가 있고 실무에서는 그것을 쓴다. 재구현은 오히려 "왜 굳이?"를 방어해야 하는 위치가 된다.
    - 근거 ②: **직전 방침의 "2.1 Cinemachine과 같은 판단"이라는 유비가 부정확했다.** 카메라는 프로젝트마다 커스텀하는 것이 흔하지만, 경로 탐색을 직접 구현하는 것은 흔하지 않다. 성격이 다른 두 결정을 같은 근거로 묶은 것이 오류였다.
    - 근거 ③: 현재 씬(`SampleScene`)은 Plane 하나에 장애물 0개다. 이 상태에서는 두 방식의 화면 결과가 동일하며, 장애물을 놓는 순간 NavMesh만이 회피를 보여줄 수 있다. 장애물 추가 비용이 사실상 0이 되는 것이 NavMesh의 실질 이득이다.
    - 셋업 비용: 빈 GameObject → `NavMeshSurface` → `Bake`. 5분 내외. `Use Geometry` 기본값이 **Render Meshes**이므로 바닥에 콜라이더가 없어도 구워진다.
- 적 이동은 **`NavMeshAgent` 전담**으로 한다. `CharacterController`를 병용하지 않는다.
    - 근거: 둘 다 transform을 쓰므로 병용하면 `updatePosition`/`updateRotation`/`nextPosition` 수동 동기화 배선이 필요해진다. 적은 아직 구현 전이므로 병용할 이유가 없다.
    - 플레이어(`PlayerMovement`)는 기존대로 `CharacterController`를 유지한다. 서로 무관하다.
- 정지 거리 경계의 **진동(떨림)은 여전히 발생할 수 있다.** `stoppingDistance` / `autoBraking` / avoidance 파라미터로 대응한다.
    - 발생 시 원인·해결·검증을 메모로 남긴다. 트러블슈팅 문서 재료다.
    - 면접 대비 핵심은 "직접 짰다"가 아니라 **"파라미터가 실제로 어떻게 동작하는지 검증하고 조정했다"**로 옮겨간다.
- 상태 전환은 `enum` + `switch`로 구현한다. **FSM 클래스 계층을 미리 만들지 않는다.**
    - 단, **나중에 FSM 클래스로 교체할 수 있도록 아래 네 가지 경계는 반드시 지킨다.** 교체 가능성은 계층이 아니라 경계로 확보된다.
        1. 전이는 반드시 `ChangeState(next)` 한 지점을 통과한다. 상태 필드 직접 대입 금지. (나중에 `OnEnter`/`OnExit` 훅이 들어갈 자리)
        2. 판단과 행동을 분리한다. `DecideNext()`(다음 상태 반환, 부작용 없음) / `Tick()`(현재 상태 행동). `switch` 두 개까지가 건강한 상한.
        3. 상태별 데이터(타이머·카운터)는 `ChangeState`에서 초기화한다. 밖으로 흩뿌리지 않는다.
        4. 외부에는 `CurrentState` 읽기 전용 프로퍼티와 `OnStateChanged` 이벤트만 노출한다. 애니메이터·스포너가 내부 구현에 의존하지 않게 한다.
    - 승격 경로 근거: Boss Room `AIBrain.cs:43-45`도 enum을 버리지 않는다. `Dictionary<AIStateType, AIState>`의 **키로 남긴다.** 지금 enum을 잘 정의해두면 그것이 그대로 승격 경로다.
    - 인터페이스를 지금 미리 만들지 않는다. 상태 4개가 실제로 무엇을 필요로 하는지 아직 모르며, 안 맞으면 두 번 고치게 된다.

---

## 4. AI 사용 원칙 (2026-07-31 확정 / **2026-08-26 개정 — 학습 모드 종료**)

> 공식 공고는 "생성형 AI 도구를 활용해 코드를 작성·검증할 수 있는 사람"을 찾는다고 명시하되, 신입이 AI가 만든 코드를 자기 실력으로 착각하는 것을 경계한다. 따라서 중요한 것은 "썼는가"가 아니라 **"검증하고 설명할 수 있는가"** 다.

### Claude Code에게 적용되는 규칙

> **2026-08-26 개정.** 직전 규칙("게임플레이 코어 구현 코드를 대신 작성하지 않는다")은 **폐기한다.**
> 그 규칙은 사용자가 3D/엔진 영역을 **학습하던 단계**의 장치였고, `guide` 스킬로 라인 단위 지시를 받아 손으로 옮겨 적는 방식이었다.
> 학습 단계는 끝났다. 지금은 작업 단계이며, `guide` 스킬은 이 프로젝트에서 제거했다.

- ✅ **구현 코드를 직접 작성한다.** 코어 영역(플레이어 이동·공격 판정·피격·사망·적 AI·상태 전환)도 포함한다.
  - 먼저 만든다. 라인 단위 지시서를 만들어 사용자가 옮겨 적게 하지 않는다.
  - 사용자가 **별도로 물어본 부분만** 설명한다. 묻지 않은 것까지 선제적으로 해설하지 않는다.
- ✅ 제한 없음: README, 회고 글, 설계 문서, 커밋 메시지, 문서 정리
- ✅ 제한 없음: 에셋 임포트, 설정, 빌드 등 `chore` 성격 작업

**판단 기준은 그대로다.** 면접에서 *"이거 왜 이렇게 구현했냐"* 를 물었을 때 사용자가 설명할 수 있어야 한다.
바뀐 것은 **기준이 아니라 그 기준을 채우는 순서**다. 사전 가이드 → 손코딩이 아니라, **구현 → 사용자 질의 → 설명**으로 확보한다.

따라서 다음은 유지된다.
- 설계 선택지가 갈리는 지점(예: `enum` + `switch` vs FSM 계층, NavMesh vs 직접 구현)은 **구현 전에 근거와 대안을 밝힌다.** 사용자가 그 판단을 설명할 수 있어야 하기 때문이다.
- 사용자가 "왜 이렇게 짰냐"고 물으면 코드 위치(`file:line`)와 함께 답한다.
- 설명할 수 없는 코드가 저장소에 남으면 이 프로젝트는 여전히 감점 요인이 된다. 그 책임은 사라지지 않았고, 확인 시점만 뒤로 옮겼다.

---

## 5. 설계 결정 문서 (핵심 산출물)

이 저장소의 **코드보다 중요한 산출물**이다. 채택/기각/근거/레퍼런스 구조를 유지한다.

1. **플레이어 구조 결정: 순수 C# 중앙화 vs 기능 단위 컴포넌트 분리** (08-01)
    - 채택: 방식 B(기능 단위 MonoBehaviour 분리 + 계산·규칙만 순수 C#)
    - 기각: 방식 A(중앙 `Player.cs` + 전면 순수 C#화 = 기존 실무 방식)
    - 근거: 이 프로젝트는 엔진 기능이 코어, 실무 프로젝트는 시뮬레이션이 코어. 조건이 반대
    - 레퍼런스: Chop Chop, Boss Room 모두 전면 순수 C# 아님

2. **공격 판정-애니메이션 타이밍 관리: 데이터 주도(hitDelay)** (08-02)
    - 채택: 방식 ②(`AttackConfig` SO의 `hitDelay` + 코루틴 타이머)
    - 기각: 방식 ①(Animation Event 주도) / 유보: 방식 ③(클라 예측 + 서버 판정)
    - 근거: 서버 권위 구조 이식성, 1인 개발에서 ①의 장점 무의미, 판정/표현 분리
    - 레퍼런스: Boss Room `ActionConfig.cs` / `MeleeAction.cs`

3. **유닛 상태 관리 권한: 분산 소유 vs 권한자 컴포넌트** (08-26) — `docs/unit-state-authority-decision.md`
    - 채택: 유닛마다 상태 소유·변경 권한을 독점하는 컴포넌트 (`PlayerState` / `EnemyState`). 상태 값 enum은 `EnemyStateType`으로 분리
    - 기각: 현행 분산 소유 / 조회 창구만 만드는 파사드
    - 근거: 상태 필드 직접 대입 지점이 흩어져 있어 상태 하나 추가에 소비처 N곳을 고쳐야 함
    - 레퍼런스: Boss Room `ServerCharacter.cs:301,346,354,382` / Chop Chop `Protagonist.cs:5,15`
    - 방식 A 회귀가 아님: 중앙화 대상이 **로직**이 아니라 **상태 소유권**

4. **(작성 예정) 카메라를 Cinemachine 없이 직접 구현한 이유**
    - 직군면접 예상 질문. 현재 결정은 있으나 문서가 없다.

5. **(조건부) 적 추적 정지 거리 진동 문제** — 실제 발생한 경우에만 작성

---

## 6. 기술 환경

- Unity (URP — PC/Mobile 렌더러 구성)
- 새 Input System (`InputSystem_Actions.inputactions`)
- 줄바꿈: 전체 LF 통일 (`.gitattributes`, `.editorconfig` 적용됨). Windows/Mac 크로스 작업 전제.
- 에셋: Synty POLYGON 계열 + RPG Character Mecanim Animation Pack + Mixamo 보강
- Burst/Collections/Mathematics는 설치되어 있으나 **사용하지 않는다** (2절 참고).

## 7. 참고 레퍼런스 프로젝트 (로컬 클론)

| 프로젝트 | 로컬 경로 | 용도 |
|---|---|---|
| Chop Chop (Unity Open Project #1) | `../open-project-1` | 아키텍처 참고 — InputReader(SO) 입력 분리, SO 이벤트, SO 상태 머신, 풀링 |
| Boss Room | `../com.unity.multiplayer.samples.coop` | 전투·어빌리티·폴더 구조 참고. **NGO 부분은 동결 범위** |

- 두 클론은 **읽기 전용**이다. 수정·커밋하지 않고, 서브모듈로 편입하지도 않는다.
- 코드를 통째로 복사하지 않는다. 패턴만 참고해 이 프로젝트 규모에 맞게 직접 구현한다.
- Chop Chop은 2021년 12월 중단된 프로젝트다. 구조는 참고하되 API는 현재 Unity 버전 기준으로 검증한다.

---

## 8. 기능 추가 판단 기준

> **2026-08-27까지는 기본값이 "추가하지 않는다"이다.** 아래 기준은 예외를 검토할 때만 쓴다.

1. 이 기능이 3D 게임플레이 역량을 보여주는가?
2. **08-27까지 끝나는가?**
3. 스크린샷/GIF에서 보이는가?
4. 면접에서 직접 설명할 수 있는가?
5. 기존 실무 경험과 중복되지 않고 약점을 보완하는가?

명확히 답하지 못하면 제외한다. **범위 확장 제안이 나오면 이 기준으로 반대 의견을 낸다.**

---

## 9. 산출물 (2026-08-27까지)

플레이 영상 1분은 **취소한다.** 웨이브·결과 화면이 없어 서사가 성립하지 않으며, 대신 짧은 GIF로 시스템 단위를 보여준다.

1. **GIF 3장**
    - 이동 + 카메라 추적
    - 적 추적 → 접근 → 공격
    - 공격 → 피격 → 사망
2. **README** — 프로젝트 성격, 구현 범위, **의도적 제외 항목과 그 이유**, 실행 방법, 조작법, 설계 문서 링크
3. **핵심 코드 위치 목록** — `PlayerMeleeAttack` / `AttackConfig` / `Health` / `PlayerHealth` / 적 AI
4. **설계 결정 문서 공개** — 5절 1·2번은 확정, 3번은 08-27 이후 작성

### README 범위 기술 (문구 확정)

```md
범위는 전투 판정 사이클까지입니다.

미구현(의도적 제외): 웨이브 진행, 보상 선택
→ UI·데이터 반영 영역은 실무 프로젝트에서 이미 다룬 영역이므로,
  제한된 기간 내에서 엔진 영역에 집중하기 위해 범위에서 제외했습니다.
```

---

## 10. 브랜치 전략

```text
main ← dev ← feature/*
```

- **main**: 항상 플레이 가능하고 보여줄 수 있는 상태만 유지한다. dev에서 **PR로만** 병합. 직접 커밋 금지.
- **dev**: 통합 브랜치. 모든 기능 개발의 출발점이자 병합 대상.
- **작업 브랜치**: dev에서 파생, 작업 하나 수행 후 dev로 병합. 짧게 유지(1~3일).
- 접두사는 5종으로 **한정**한다: `feature/` `fix/` `refactor/` `chore/` `docs/`
- **08-27 최종 마감 시 dev → main PR을 만들고 병합한다.** 공개 저장소의 기본 브랜치가 최신 상태여야 한다.
- 머신(Windows ↔ Mac) 전환 전에는 작업 중인 브랜치를 반드시 push한다.

## 11. 워크트리 병렬 작업과 서브모듈

- 본진(`3DActionShooter`)은 사용자 전용, `-wt1`/`-wt2`는 Claude 병렬 세션용 반영구 워크트리다. 제거하지 않는다.
- `Assets/ThirdParty` 서브모듈은 워크트리마다 별도 클론이 정상이다. junction/폴더 공유 금지 — 진실은 각 브랜치의 포인터이고, 체크아웃은 파생물이다.
- 브랜치 전환/pull/push 시 서브모듈은 자동 동기화된다 (`submodule.recurse`, `push.recurseSubmodules on-demand`).
- 에셋 수정은 한 체크아웃에서만 한다. 나머지 워크트리에서 ThirdParty는 읽기 전용이며, 전파는 서브모듈 push → 부모 `chore` 커밋(포인터 갱신)으로만 한다.
- 새 워크트리 생성 시 1회 필수: `git submodule update --init --reference E:\Unity\3DActionShooter\Assets\ThirdParty`

## 12. AgentDoc 규칙

`[AgentDoc]`은 코드의 목적·수정 시 주의사항을 사람이 남기고 AI가 읽는 어트리뷰트다 (`Assets/Scripts/Core/AgentDocModule/AgentDocAttribute.cs`). 조회는 `agent-doc` 스킬로 한다.

- **`[AgentDoc]`은 사람만 수동 작성한다. AI는 추가·수정·삭제 금지.** 유일한 예외: 사용자가 직접 `/agent-doc-bootstrap`을 호출한 경우 AI의 **신규 추가만** 허용.
- 코드 수정 전 대상 영역에 `warning`이 있으면 반드시 읽는다.
- **Scripting Define Symbols에 `AGENT_DOC`을 추가하지 않는다** — 어트리뷰트 문자열이 출시 바이너리에 유출된다. 미정의가 정상이며 스캐너는 심볼과 무관하게 소스 텍스트를 읽는다.
- `.llm-index/`는 스캔 산출물이다 (gitignore 대상). 커밋하지 않고 사용 직전 재스캔으로만 최신화한다.

---

## 13. 관련 문서의 유효성

| 문서 | 상태 |
|---|---|
| **CLAUDE.md (이 문서)** | **현행. 범위·일정·원칙의 유일한 기준** |
| `planning-guideline.md` | 동결. 2026-07-15 초기 기획 원본이며 탑다운 전제라 현재와 어긋난다 |
| `포폴_3D_todo.md` | 진행 로그용. 08-27 마감 시점에 최종 갱신 |
| `넥토리얼_준비_계획표.md` | 코딩테스트 전제로 작성되어 1번 축이 무효. 서류 제출 후 재작성 |

**충돌 시 이 문서를 따른다.**

---

## 14. 작업 원칙

- **범위를 넓히지 않는다.** 08-27까지 기본값은 "추가하지 않는다"이다.
- 코어 구현 코드는 사용자가 직접 작성한다 (4절).
- 설명할 수 없는 코드는 저장소에 넣지 않는다.
- 미구현은 감추지 않고 이유와 함께 명시한다.
- 과도한 추상화를 만들지 않는다. `enum` + `switch`로 충분한 곳에 계층을 만들지 않는다.
- 기획이 흔들리면 원칙으로 돌아간다: **전투 판정 사이클까지. 08-27 종료. 보조 프로젝트.**
