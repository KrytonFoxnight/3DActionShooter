# CLAUDE.md

이 문서는 Claude Code가 이 저장소에서 작업할 때 따라야 할 프로젝트 지침이다.

## 프로젝트 개요

**Descendant Arena Lite (가칭)** — 2026년 9~10월 넥토리얼(넥슨) Unity/C# 클라이언트 직무 지원용 포트폴리오 데모.

- 넥슨게임즈 퍼스트 디센던트를 **구조적 레퍼런스**로 삼은 3D 탑다운 무기(활/검) 액션 RPG
- 싱글 플레이, 3~5분 플레이어블 데모
- 원작의 IP/캐릭터/아트/UI/명칭/세계관은 모방하지 않는다. 참고하는 것은 **PvE 전투 → 보상 → 강화 → 다음 전투** 게임 루프 구조뿐이다.

### 프로젝트의 존재 이유

사용자의 기존 실무 강점은 UGUI, MVVM/ViewController, VContainer, 상점/IAP, 효과 시스템, 테스트 코드, CI/CD 등 라이브 서비스형 클라이언트 시스템이다. 이 프로젝트는 그 강점을 반복하는 것이 아니라, 공개 증거가 부족한 영역을 보완한다:

- 3D 공간 활용, 캐릭터 이동, 물리/충돌/피격 판정
- 카메라 제어, 애니메이션 상태 전환
- 적 AI, 웨이브/스폰 시스템, 플레이 가능한 전투 루프

**따라서 UI/운영 기능이 아니라 3D 게임플레이 코어가 항상 최우선이다.**

## 기술 환경

- Unity (URP — PC/Mobile 렌더러 구성)
- 새 Input System (`InputSystem_Actions.inputactions`)
- 줄바꿈: 전체 LF 통일 (`.gitattributes`, `.editorconfig` 적용됨). Windows/Mac 크로스 작업 전제.
- 에셋 방향: Synty POLYGON 계열 아트 + RPG Character Mecanim Animation Pack + Mixamo 보강

## 고정 게임 루프

```text
타이틀 → 아레나 진입 → Wave 1 → 보상 선택 → Wave 2 → 보상 선택
→ Wave 3 (근접 다수 + 원거리 일부 + 미니 보스) → 결과 화면
```

## 고정 기획안

### 플레이어 (1명 고정)

- 3D 탑다운 이동, 공격 방향 바라보기, 기본 공격(활: 투사체 / 검: 전방 히트박스)
- 대시, 스킬 2개(범위 폭발 + 생존/회피형), HP, 피격, 사망

### 적 (최소 구성 고정)

- 근접 추적형 1종 (필수) — 추적 → 근접 시 공격
- 원거리 공격형 1종 (시간 부족 시 제외 가능)
- 미니 보스 1종 — HP 많음, 공격 패턴 2개 이하, 마지막 웨이브 등장

### 웨이브

- Wave 1: 근접 5마리 (조작 학습)
- Wave 2: 근접 8 + 원거리 2 (압박 증가)
- Wave 3: 근접 다수 + 원거리 일부 + 미니 보스 (클라이맥스)

### 보상 (웨이브 클리어 시 3택1)

공격력 / 공격 속도 / 이동속도 / 최대 HP / 스킬 쿨감 / 투사체 크기 / 대시 쿨감 중 5종 내외.
핵심은 종류 수가 아니라 **선택한 보상이 다음 웨이브에서 체감되는 것**.

## 일정 (마감: 2026-08-31, 약 6.7주)

| 주차 | 목표 | 완료 기준 |
|---|---|---|
| 1주 | 이동 + 카메라 | 플레이어가 움직이고 카메라가 따라온다 |
| 2주 | 전투 기본 | 서로 공격해서 죽고 죽일 수 있다 |
| 3주 | 적 AI + 웨이브 | 3웨이브 플레이, 승리/패배 결과가 나온다 |
| 4주 | 보상/성장 | 보상 선택이 다음 웨이브에서 체감된다 |
| 5주 | 엔진 어필 폴리싱 | Animator/풀링/VFX — 영상으로 게임처럼 보인다 |
| 6주 | 문서화/영상 | GitHub, 영상, README를 채용자에게 보여줄 수 있다 |
| +0.7주 | 버퍼 | 기능 추가 금지. 완성도 확보 전용 |

## 기능 우선순위

- **반드시**: 플레이어 이동, 카메라 follow, 기본 공격, 피격/사망, 근접 적 AI, 웨이브 3개, 보상 선택, 결과 화면, 플레이 영상, README
- **가능하면**: 원거리 적, 대시, 범위 스킬, Object Pooling, Animator, 카메라 shake, 간단한 VFX, 전투 로그
- **시간 남으면**: 인벤토리, 상점, Addressables, 테스트 코드, Mock API, CI 검증
- **하지 말 것**: IAP, Firestore, 우편함, 출석 보상, 복잡한 라이브 서비스 기능, 많은 캐릭터/스킬, 여러 보스, 과도한 아키텍처 추상화

## 기능 추가 판단 기준

기능을 추가할지 고민될 때 다음 질문에 명확히 답하지 못하면 제외한다:

1. 이 기능이 3D 게임플레이 역량을 보여주는가?
2. 9월 전 완성 가능성을 높이는가, 낮추는가?
3. 플레이 영상에서 보이는가?
4. 넥토리얼 Unity/C# 클라이언트 지원에 설명할 수 있는가?
5. 기존 실무 경험과 중복되지 않고 약점을 보완하는가?

## 보완 역량: 네트워크 / 멀티스레딩

기획 가이드라인 외에 이 프로젝트에서 의식적으로 챙길 기술 역량 2개. (넥토리얼 요구 역량 대응)

### 네트워크 — 2단계 분리 전략 (확정)

- **1단계 (~8/31)**: 싱글 플레이 데모를 완성한다. 네트워크 코드는 넣지 않는다.
- **2단계 (9월, 데모 완성 후)**: Netcode for GameObjects 기반 2인 코옵 확장. Multiplayer Center(설치됨)의 추천 스택을 따른다.
- **단, 1단계부터 아래 "네트워크 대비 설계 훅" 3개를 지킨다.** 이는 2단계 전환 비용을 낮추기 위함이며, 그 자체로 좋은 구조다:
  1. 입력 처리와 행동 로직을 분리한다 (입력 → 의도/커맨드 → 실행).
  2. 데미지/스탯 계산은 MonoBehaviour에 얽매이지 않는 순수 C# 레이어로 둔다.
  3. 적/투사체 스폰은 팩토리(또는 단일 스포너 창구)를 거친다.
- 이 훅을 넘어서는 선제적 네트워크 추상화는 금지 (과도한 추상화 금지 원칙).

### 멀티스레딩 — Job System + Burst (기회 기반 적용)

- 특정 시스템에 고정하지 않는다. 기획 가이드라인의 구현 항목을 진행하면서 **다수 오브젝트의 배치 계산이 나타나는 지점**에 기회 기반으로 적용한다.
- 유력 후보 (우선순위 순): Wave 3 다수 적의 이동/분리 조향 계산, 투사체 일괄 이동/수명 처리.
- 적용 시 반드시 프로파일러 before/after를 기록한다 — README/회고 글의 증거 자료가 된다.
- Burst/Collections/Mathematics는 프로젝트에 이미 존재한다. 추가 설치 불필요.
- 판단 기준: 메인 스레드에서 충분히 싼 계산에 Jobs를 억지로 넣지 않는다. "왜 여기에 썼는가"를 면접에서 답할 수 있는 지점에만 적용한다.

## 브랜치 전략

```text
main ← dev ← feature/*
```

- **main**: 거시적 안정 브랜치. 항상 플레이 가능하고 보여줄 수 있는 상태만 유지한다. dev에서 **PR로만** 병합된다. 직접 커밋 금지.
- **dev**: 통합 브랜치. 모든 기능 개발의 출발점이자 병합 대상.
- **작업 브랜치**: dev에서 파생해 작업 하나를 수행하고 dev로 병합한다. 짧게 유지한다 (1~3일).
- 브랜치 접두사는 다음 5종으로 **한정**한다. 새 접두사를 임의로 만들지 않는다.
  - `feature/` — 신규 기능 (예: `feature/player-movement`)
  - `fix/` — 버그 수정 (예: `fix/dash-collision`)
  - `refactor/` — 동작 변화 없는 구조 개선 (예: `refactor/enemy-state-machine`)
  - `chore/` — 설정, 에셋 임포트, 빌드 등 잡무 (예: `chore/import-synty-pack`)
  - `docs/` — 문서 (예: `docs/readme-controls`)
- 안정 지점(주차 마일스톤 등)에 도달하면 dev → main PR을 만든다. PR에는 사용자를 assignee로 지정한다.
- 머신(Windows ↔ Mac) 전환 전에는 작업 중인 브랜치를 반드시 push한다.

## 작업 원칙

- 콘텐츠 양보다 **게임 루프의 완결성**을 우선한다.
- 영상으로 보여줄 수 있는 장면을 우선한다.
- 범위 확장 제안이 나오면 위 판단 기준으로 반대 의견을 낸다.
- 기획이 흔들리면 원칙으로 돌아간다: 퍼스트 디센던트-lite 루프, 3D 탑다운 무기 액션, 싱글 3~5분 데모.

## 산출물 (9월 전 필수)

1. GitHub README — 프로젝트 소개, 실행 방법, 조작법, 시스템 구조, 트러블슈팅, 영상 링크
2. 플레이 영상 1분 내외 — 이동/카메라/공격/피격/적 추적/웨이브/보상/강화 체감/결과 화면
3. 코드 샘플 — 플레이어 상태 처리, 피격 판정, 적 AI, 웨이브 스폰, 보상 반영, 풀링
4. 기술 회고 글
